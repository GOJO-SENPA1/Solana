{
  "name": "solona-chainremit",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@11.11.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm -w apps/relayer run dev & npm -w apps/web run dev",
    "dev:web": "npm -w apps/web run dev",
    "dev:relayer": "npm -w apps/relayer run dev",
    "build": "npm -w apps/relayer run build && npm -w apps/web run build",
    "lint": "npm -w apps/relayer run lint && npm -w apps/web run lint"
  }
}

